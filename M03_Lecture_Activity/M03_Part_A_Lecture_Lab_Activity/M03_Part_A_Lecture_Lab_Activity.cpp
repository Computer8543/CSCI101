// M03_Part_A_Lecture_Lab_Activity.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include <string>
#include <cassert>

using namespace std;

int main()
{
	/*
	int age;
	cout << "Enter your age: " << endl;
	cin >> age;
	if (age >= 18)
		cout << "You are eligible to vote." << endl;
	else
		cout << "You are not eligible to vote." << endl;
	cout << "The End" << endl;
	

	// convert score to a letter grade
	int score;
	cout << "Enter a score " << endl;
	cin >> score;
	
	if (score >= 90)
		cout << "A" << endl;
	else if (score >= 80 && score < 90)
		cout << "B" << endl;
	else if (score >= 70 && score < 80)
		cout << "C" << endl;
	else if (score >= 60 && score < 70)
		cout << "D" << endl;
	else
		cout << "F" << endl;

	
	int score;
	cout << "Enter a score " << endl;
	cin >> score;
	if (!(score > 50)) // note: use double parenthesis when using a NOT operator like with (!(score > 50))
		cout << "You passed!" << endl;
	cout << "Then end" << endl;		

	
	char grade;
	cout << "Enter a grade: " << endl;
	cin >> grade;

	switch (grade)
	{
		case 'A':
			cout << "The grade point average is 4.0." << endl;
			break;
		case 'B':
			cout << "The grade point average is 3.0." << endl;
			break;
		case 'C':
			cout << "The grade point average is 2.0." << endl;
			break;
		case 'D':
			cout << "The grade point average is 1.0." << endl;
			break;
		case 'F':
			cout << "The grade point average is 0.0." << endl;
			break;
		default:
			cout << "The grade is invalid." << endl;
	}

	char grade;
	cout << "Enter a grade: " << endl;
	cin >> grade;

	if (grade == 'A') // use single instead of double quotes for char data type!
		cout << "The grade point average is 4.0." << endl;
	else if (grade == 'B')
		cout << "The grade point average is 3.0." << endl;
	else if (grade == 'C')
		cout << "The grade point average is 2.0." << endl;
	else if (grade == 'D')
		cout << "The grade point average is 1.0." << endl;
	else if (grade == 'F')
		cout << "The grade point average is 0.0." << endl;
	else 
		cout << "The grade is invalid." << endl;

	*/
}

// Run program: Ctrl + F5 or Debug > Start Without Debugging menu
// Debug program: F5 or Debug > Start Debugging menu

// Tips for Getting Started: 
//   1. Use the Solution Explorer window to add/manage files
//   2. Use the Team Explorer window to connect to source control
//   3. Use the Output window to see build output and other messages
//   4. Use the Error List window to view errors
//   5. Go to Project > Add New Item to create new code files, or Project > Add Existing Item to add existing code files to the project
//   6. In the future, to open this project again, go to File > Open > Project and select the .sln file
